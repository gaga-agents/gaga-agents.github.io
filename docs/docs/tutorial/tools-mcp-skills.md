---
title: 工具、MCP 与 Skills
description: 区分内置工具、机理模型工具、外部 Skill 与协议适配。
---

# 工具、MCP 与 Skills

平台把能力分成三类：内置文件与工作空间工具、工具管理服务中的可复用工具包，以及外部通用 Skills。MCP 或其他协议适配属于连接外部能力的实现方式，不应与业务工具本身混为一谈。

## 能力选择

| 类型 | 适合 | 配置位置 | 注意事项 |
| --- | --- | --- | --- |
| 内置工具 | 文件、文本、PDF、表格、工作空间操作 | `enable_builtin_tools` | 默认开启，需限制文件范围 |
| 单个工具 | 一个明确的机理或业务操作 | `individual_tools` | 参数 Schema 必须稳定 |
| 工具库 | 一组可一起复用的相关工具 | `tool_libraries` | 避免把无关工具全部暴露给模型 |
| 外部 Skill | 跨项目复用的高层能力 | `external_skills` | 固定版本快照并观察副作用 |
| MCP 适配 | 对接外部 MCP Server | 协议/网关适配层 | 先验证权限、超时和结果格式 |

## 工具调用约定

一个工具至少需要名称、描述、参数结构、成功结果和失败分类。描述要告诉模型“什么时候调用”，也要告诉它“什么时候不要调用”。高风险或不可逆操作应增加确认步骤；后端已经提供工具确认服务和 `tool_call_pending_confirmation` 等事件供前端展示。

## 测试顺序

先直接调用工具，确认参数校验和错误处理；再让智能体自主选择工具；最后在多工具并存时验证选择边界。每次只加入一个新能力，才能把问题归因到具体工具或 Prompt。
